# Vite picks up VITE_* env vars at build time.
# After running `npx partykit deploy` from /partykit, paste the resulting host here.
# Leave empty for local dev (defaults to 127.0.0.1:1999).
VITE_PARTYKIT_HOST=
