# game-specs

One markdown file per approved game spec, named `<id>.md`. These are the long-form, human-readable specs that Claude Code reads when building a game. The structured/canonical version of each spec lives in `pipeline/games.json` and conforms to `pipeline/game-schema.json`.

Add a new spec when you flip a game's status from `brainstormed` → `approved`.
